# AI Gateway docs (Blume)

AI-ready docs for free-ai / AI Gateway.

```bash
npm install && npm run build
```

Cut over a Pages project (recommended domain `docs.ai-gateway.sassmaker.com`)
when ready. Content synced from `site/src/content/docs`.
